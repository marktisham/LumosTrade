steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - '${_IMAGE_URL}'
      - '--build-arg'
      - 'NODE_ENV=${_NODE_ENV}'
      - '-f'
      - 'LumosAI/Tools/LumosTradeTool/Dockerfile'
      - '.'
images:
  - '${_IMAGE_URL}'
