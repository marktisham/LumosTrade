# Lumos Environment Configuration Template
# Copy this file to <environment>.env (e.g., development.env, production.env, staging.env)
# and fill in the values below for your environment.
# Format: KEY=VALUE (no spaces around =, no export statements)
#
# Variable expansion: This file uses ${VAR} syntax for variable references.
# - Run './lumos' to automatically generate the expanded version
# - Shell scripts and VS Code launch.json both use the expanded file
#

###############################################################
# User configured variables
# SET THESE FOR YOUR ENVIRONMENT
###############################################################

# Environment name (must match the filename without .env extension)
# Examples: development, production, staging, testing
LUMOS_ENVIRONMENT=YOUR_ENVIRONMENT_NAME_HERE

# GCP Project ID
# To list available projects: gcloud projects list
# To get current project: gcloud config get-value project
PROJECT_ID=YOUR_PROJECT_ID_HERE

# GCP Project Number
# To get project number: gcloud projects describe YOUR_PROJECT_ID_HERE --format="value(projectNumber)"
PROJECT_NUMBER=YOUR_PROJECT_NUMBER_HERE

# GCP Region to use for Cloud Run, Cloud SQL, etc.
# To list available regions: gcloud compute regions list
# Common options: us-east4, us-central1, us-west1, europe-west1
REGION=YOUR_REGION_HERE

###############################################################
# System configuration.
# You generally don't need to change these, unless you want to
# customize something very specific.
###############################################################

# Cloud SQL Instance
# Default naming: lumos-${LUMOS_ENVIRONMENT}
SQL_INSTANCE=lumos-${LUMOS_ENVIRONMENT}
SQL_DATABASE=lumos
SQL_REGION=${REGION}

# GCP project ID where broker access tokens are stored in Datastore
# Useful for broker testing since you often need to use their live API in your development environment.
# (set to your production GCP project ID in that scenario, otherwise use ${PROJECT_ID})
BROKER_ACCESS_DATASTORE_PROJECT_ID=${PROJECT_ID}

# Secret Manager secret name
LUMOS_SECRET_NAME=lumos

# Tool Settings
TOOL_SERVICE_ACCOUNT=lumostool@${PROJECT_ID}.iam.gserviceaccount.com
TOOL_LUMOSDB_SERVICE_NAME=lumos-tool-lumosdb
TOOL_LUMOSDB_SERVICE_URL=https://${TOOL_LUMOSDB_SERVICE_NAME}-${PROJECT_NUMBER}.${REGION}.run.app
TOOL_LUMOSTRADE_SERVICE_NAME=lumos-tool-trade
TOOL_LUMOSTRADE_SERVICE_URL=https://${TOOL_LUMOSTRADE_SERVICE_NAME}-${PROJECT_NUMBER}.${REGION}.run.app

# Agent Settings
AGENT_SERVICE_ACCOUNT=lumosagent@${PROJECT_ID}.iam.gserviceaccount.com
AGENT_LUMOSAGENTS_SERVICE_NAME=lumos-agents
AGENT_LUMOSAGENTS_URL=https://${AGENT_LUMOSAGENTS_SERVICE_NAME}-${PROJECT_NUMBER}.${REGION}.run.app
AGENT_LUMOSAGENTS_CHAT=LumosChatAgent
AGENT_LUMOSAGENTS_CONJURE=LumosConjureAgent

# Lumos App Service Settings
LUMOS_APP_SERVICE_ACCOUNT=lumosapp@${PROJECT_ID}.iam.gserviceaccount.com
LUMOS_APP_SERVICE_NAME=lumos-app
LUMOS_APP_SERVICE_URL=https://${LUMOS_APP_SERVICE_NAME}-${PROJECT_NUMBER}.${REGION}.run.app

# Use Vertex AI for Google Gen AI SDK (1 = use Vertex AI, 0 = use standard AI API)
GOOGLE_GENAI_USE_VERTEXAI=1

# Demo Mode - When enabled, all broker API calls return simulated data instead of live broker data
# Set to True to enable demo mode, or False to use live broker APIs
DEMO_MODE=False
# Allow edit operations in demo mode (True to allow add/update/delete)
DEMO_ALLOW_EDITS=False

# Maximum number of days to look back for initial account import
# Subject to broker limitations (typically 2 years or less)
# Uncomment to override the default (may be faster/cleaner imports for old accounts)
MAX_IMPORT_DAYS=400

# GitHub Repository URL
# Used in footer links to LICENSE, DISCLAIMER, and source code
GITHUB_REPO_URL=https://github.com/marktisham/LumosTrade

