<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : 'LumosApp' %></title>
  <link rel="stylesheet" href="/bootstrap/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/styles.css">
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
  <script>
    window.LUMOS_DEMO_MODE = <%= typeof demoMode !== 'undefined' ? demoMode : false %>;
    window.LUMOS_DEMO_ALLOW_EDITS = <%= typeof demoAllowEdits !== 'undefined' ? demoAllowEdits : false %>;
  </script>
</head>
<body class="bg-dark text-light">
  <header>
    <% if (typeof showNavbar !== 'undefined' && showNavbar) { %>
      <% if (typeof inZeroState !== 'undefined' && inZeroState) { %>
        <%- include('../templates/navbar-zero.ejs') %>
      <% } else { %>
        <%- include('../templates/navbar.ejs') %>
      <% } %>
    <% } %>
  </header>
  <main style="padding-bottom: 1.5rem;">
    <div class="container pt-3">
      <%- body %>
    </div>
  </main>
  <footer class="text-center mt-1 mb-1 text-secondary border-top border-secondary pt-3 pb-4" style="padding-bottom: 2.5rem;">
    <small>
      Environment: <%= environment %>. Build: 
      <% if (typeof buildNumber !== 'undefined' && buildNumber) { %>
      <%= buildNumber %>.
      <% } else { %>
      localhost.
      <% } %>
      Lisham al-Gaib.
    </small>
    <div class="mt-2 mx-auto" style="font-size: 0.7rem; opacity: 0.7; line-height: 1.4; max-width: 50%; white-space: normal; word-wrap: break-word; padding-bottom: 0.5rem; text-align: center;">
      <div>
        Lumos Trade is an open-source research tool powered by Vertex AI.
      </div>
      <div>
        Risk Warning: Trading involves significant risk of loss. AI agents can hallucinate or fail. No content here constitutes financial advice.
      </div>
      <div class="mt-1">&copy; 2026 Lumos Trade / Mark Isham</div>
      <div class="mt-1">
      <% if (typeof githubRepoUrl !== 'undefined' && githubRepoUrl) { %>
      <a href="<%= githubRepoUrl %>/blob/main/LICENSE" target="_blank" rel="noopener" class="text-secondary">License</a> |
      <a href="<%= githubRepoUrl %>/blob/main/DISCLAIMER.md" target="_blank" rel="noopener" class="text-secondary">Disclaimer</a> |
      <a href="<%= githubRepoUrl %>" target="_blank" rel="noopener" class="text-secondary">GitHub</a>
      <% } else { %>
      <a href="/LICENSE" class="text-secondary">License</a> |
      <a href="/DISCLAIMER.md" class="text-secondary">Disclaimer</a> |
      GitHub
      <% } %>
      </div>
    </div>
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
