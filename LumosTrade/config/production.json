{
  "// COMMENT": "This file contains environment-specific overrides for the production environment.",
  "// COMMENT2": "Values defined here override those in default.json when NODE_ENV is set to 'production'.",
  "environment": "production"
}
